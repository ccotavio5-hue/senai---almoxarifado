CREATE DATABASE tcc;
USE tcc;

CREATE TABLE estoque (
    id INT AUTO_INCREMENT PRIMARY KEY,
    item VARCHAR(255),
    descricao VARCHAR(255),
    quantidade INT,
    imagem VARCHAR(255)
);

CREATE TABLE historico (
    id INT AUTO_INCREMENT PRIMARY KEY,
    item VARCHAR(255),
    quantidade INT,
    pessoa VARCHAR(255),
    data_hora DATETIME DEFAULT now()
);

CREATE TABLE administrador (
    id INT AUTO_INCREMENT PRIMARY KEY,
    usuario VARCHAR(180) NOT NULL,
    senha VARCHAR(250)

);
INSERT INTO administrador (usuario, senha) 
VALUES ('roger', '$2b$12$4traD3hZntpE1sjyW1HDr.y0GDOdmaKha23.5/mwo6N/PsDnjuC12');

CREATE TABLE usuario (
    id INT AUTO_INCREMENT PRIMARY KEY,
    usuario VARCHAR(180) NOT NULL,
    senha VARCHAR(255)
);

SELECT * FROM estoque;
SELECT * FROM historico;
SELECT * FROM usuario;
SELECT * FROM administrador;

DROP DATABASE tcc;

python senhaloka.py